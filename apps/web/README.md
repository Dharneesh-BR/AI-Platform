# Web Application

Next.js App Router frontend for the AI consulting platform.

State management rules:

- TanStack Query owns server state.
- Zustand owns client-only interaction state.
- React Hook Form owns form state.
- Zod owns runtime validation.
- shadcn/ui is the only UI component foundation.

