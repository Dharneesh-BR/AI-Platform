# Contracts Package

Shared API contracts, DTO schemas, and Zod validators used by frontend and backend.

Contracts are allowed to depend on primitive shared utilities only. They must not depend on application or infrastructure code.

